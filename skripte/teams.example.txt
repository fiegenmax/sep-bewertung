# Teamliste fuer gen_mapping.py
#
# Kopiere diese Datei zu teams.txt und trage die Teams ein (eines pro Zeile).
# teams.txt ist in .gitignore und wird nicht committet (zeigt auf Studi-Repos).
#
# Format pro Zeile ('#' = Kommentar, Leerzeilen werden ignoriert):
#
#   <cohort>-<kurzname>        Der kombinierte GitLab-Name, mit oder ohne
#                              fuehrendes "team-". Der Kurzname (Teil nach dem
#                              ersten "-") ist eindeutig und wird zum lokalen
#                              Ordnernamen.
#
# Daraus entsteht:
#   GitLab-Projekt: {GITLAB_GROUP}/team-<cohort>-<kurzname>
#   lokaler Ordner: team-<kurzname>
#
# Beispiel: "shannon-alpha"
#   -> GitLab: .../student_projects/team-shannon-alpha
#   -> lokal:  team-alpha
#
# Teams aus verschiedenen Tutorien/Kohorten stehen einfach mit ihrem jeweiligen
# Cohort-Praefix in derselben Liste.

shannon-alpha
shannon-beta
team-shannon-gamma

# Team in einer anderen Kohorte: einfach deren Cohort-Praefix verwenden
lovelace-delta
